import type { FeatureFlags } from "@/types";

export const defaultFeatureFlags: FeatureFlags = {
  newsletter: true,
  comments: false,
  chatAI: false,
  login: false,
  studentArea: false,
  landingPages: true,
  banner: true,
  cta: true,
  darkMode: false,
};